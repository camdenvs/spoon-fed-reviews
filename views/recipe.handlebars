<!--when people select a specific recipe to look at-->
<div>
  <div>
    <h2>{{name}}</h2>
  </div>
  <div>
    <b>Ingredients: </b>
    <p>
    <pre>{{ingredients}}</pre>
    </p>
    <b>Instructions: </b>
    <p>
    <pre>{{instructions}}</pre>
    </p>
    <p><b>Created by: </b> {{recipe.user.name}} {{recipe.date_posted}}</p>

    <div>
      <form class="field" id="new-comment-form" data-id="{{id}}">
        <label class="label">Leave a comment:</label>
        <div class="control">
          <textarea class="textarea" placeholder="Leave a comment" id="comment-body"></textarea>
        </div>
        <div class="control">
          <button class="button is-link">Submit</button>
        </div>
        <label class="checkbox">
          <input type="checkbox">
          Upvote recipe
        </label>
      </form>
    </div>
    {{#each comments as |comment| }}
    <div class="content">
      <h2>{{user.name}}</a></h2>
      <p>{{text}}</p>
    </div>
    {{/each}}
  </div>
</div>

<script src="../js/recipe.js"></script>